import React from 'react';

const Navigation = () => {
    return(
        <nav style = {{display: 'flex', justifycontent: 'flex-end'}}>
            <p className='f3 link dim black underlinr pa3 pointer'>Sign Out</p>
        </nav>
    );
}

export default Navigation;